version: '3.7'

networks:
  traefik_services:
    external: true

services:

  schema-registry-ui:
#    image: landoop/schema-registry-ui:0.9.5
    image: landoop/schema-registry-ui:latest
    environment:
      SCHEMAREGISTRY_URL: http://schema-registry.internal.test:8085/
      PROXY: "true"
    depends_on:
      - schema-registry
    hostname: schema-registry-ui.internal.test
    networks:
      default:
        aliases:
          - schema-registry-ui.internal.test
      traefik_services:
        aliases:
          - schema-registry-ui.internal.test
    labels:
      - "traefik.enable=true"
      - "traefik.http.services.schema-registry-ui.loadbalancer.server.port=8000"
      - "traefik.http.routers.schema-registry-ui.rule=Host(`schema-registry-ui.${DOMAIN_SUFFIX:-external.test}`)"
      - "traefik.http.routers.schema-registry-ui.entrypoints=websecure"
      - "traefik.http.routers.schema-registry-ui.tls=true"